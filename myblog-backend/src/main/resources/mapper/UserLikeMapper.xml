<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ryan.myblog.mapper.UserLikeMapper">

    <!-- 根据用户ID和目标类型、目标ID查询点赞记录 -->
    <select id="selectByUserAndTarget" resultType="com.ryan.myblog.entity.UserLike">
        SELECT * FROM tb_user_like
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND deleted = 0
        LIMIT 1
    </select>

    <!-- 统计目标点赞数量 -->
    <select id="countByTarget" resultType="java.lang.Long">
        SELECT COUNT(*) FROM tb_user_like
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          AND status = 1
          AND deleted = 0
    </select>

</mapper>