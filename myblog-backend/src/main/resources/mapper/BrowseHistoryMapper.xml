<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ryan.myblog.mapper.BrowseHistoryMapper">

    <!-- 插入或更新浏览记录 -->
    <insert id="insertOrUpdate">
        INSERT INTO tb_browse_history (user_id, blog_id, browse_time, create_time, update_time)
        VALUES (#{userId}, #{blogId}, NOW(), NOW(), NOW())
        ON DUPLICATE KEY UPDATE
            browse_time = NOW(),
            update_time = NOW()
    </insert>

</mapper>
