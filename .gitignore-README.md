# .gitignore 配置说明

## 更新日期
2025-12-03

## 主要变更

### 问题修复
1. ❌ **旧版本问题**：忽略了所有 `.sh`、`.md`、`.json` 文件，导致重要文件无法提交
2. ✅ **新版本**：精确控制忽略规则，确保敏感文件被忽略，重要文件可以提交

## 忽略规则说明

### 🔒 被忽略的文件（不会提交到Git）

#### 敏感配置文件
```
application-local.yml          # 本地开发配置（包含本地数据库密码等）
application-dev.yml            # 开发环境配置
.env.prod                      # 生产环境变量（包含MySQL、Redis密码等）
.env.local                     # 本地环境变量
```

#### 临时文件和缓存
```
dump.rdb                       # Redis数据文件
*.log                          # 日志文件
logs/                          # 日志目录
*.tmp, *.bak                   # 临时和备份文件
.DS_Store, Thumbs.db          # 操作系统文件
```

#### 运行时数据
```
data/                          # 数据目录
uploads/                       # 上传文件目录
backups/                       # 备份目录
```

#### 构建产物
```
target/                        # Maven构建产物
node_modules/                  # npm依赖包
dist/                          # 前端构建产物
*.class, *.jar, *.war         # Java编译文件
```

#### IDE和工具
```
.idea/                         # IntelliJ IDEA配置
.vscode/                       # VS Code配置
.playwright-mcp/              # Playwright缓存
```

### ✅ 可以提交的文件

#### 部署脚本
```
deploy/*.sh                    # 所有部署脚本
  - server-setup.sh            # 服务器初始化
  - init-database.sh           # 数据库初始化
  - deploy.sh                  # 应用部署
  - quick-deploy.sh            # 一键部署
  - backup.sh                  # 数据备份
  - logs.sh                    # 日志查看
  - stop.sh                    # 停止服务
  - create-env.sh              # 创建环境变量
```

#### 文档文件
```
README.md                      # 项目说明
DEPLOYMENT.md                  # 部署文档
deploy/*.md                    # 部署相关文档
  - QUICK_START.md             # 快速开始
  - SETUP_GUIDE.md             # 详细指南
  - CHECKLIST.md               # 检查清单
  - ENV_SETUP.md               # 环境配置
```

#### 配置文件
```
package.json                   # npm依赖配置
package-lock.json              # npm锁定文件
tsconfig.json                  # TypeScript配置
pom.xml                        # Maven配置
docker-compose*.yml            # Docker编排配置
Dockerfile                     # Docker镜像配置
nginx.conf, myblog.conf       # Nginx配置
application.yml                # Spring Boot主配置
application-prod.yml           # 生产配置（使用环境变量引用）
```

## 特殊说明

### application-prod.yml 为什么可以提交？

`application-prod.yml` 文件使用环境变量引用，**不包含任何敏感信息**：

```yaml
spring:
  datasource:
    password: ${MYSQL_PASSWORD}      # 引用环境变量
    
jwt:
  secret: ${JWT_SECRET}              # 引用环境变量
```

实际的密码和密钥存储在 `.env.prod` 文件中，该文件**被忽略不会提交**。

### application-local.yml 为什么要忽略？

`application-local.yml` 包含本地开发的实际配置，可能包含：
- 本地数据库密码
- 本地服务地址
- 开发者个人设置

这些信息对其他开发者没有用，且可能暴露敏感信息。

## 验证配置

### 检查文件是否被忽略

```bash
# 检查某个文件是否被忽略
git check-ignore -v filename

# 例如：
git check-ignore -v myblog-backend/src/main/resources/application-local.yml
# 应该输出忽略规则

git check-ignore -v myblog-backend/src/main/resources/application-prod.yml
# 应该输出: (no output) 或表示不被忽略
```

### 查看可提交的新文件

```bash
# 查看未跟踪的文件
git status --short | grep "^??"
```

### 查看被忽略的文件

```bash
# 查看所有被忽略的文件
git status --ignored
```

## 最佳实践

### 添加新文件时

1. **敏感文件**：确保包含密码、密钥的文件被忽略
2. **配置模板**：可以提交 `.example` 或 `.template` 后缀的模板文件
3. **环境变量**：使用 `${ENV_VAR}` 引用，不要硬编码

### 部署到服务器时

1. 在服务器上创建 `.env.prod` 文件（不会从Git拉取）
2. 在服务器上创建 `application-local.yml`（如果需要本地覆盖）
3. 确保这些文件的权限设置为 600（仅所有者可读写）

```bash
chmod 600 .env.prod
chmod 600 myblog-backend/src/main/resources/application-local.yml
```

## 故障排查

### 问题：想提交的文件被忽略了

```bash
# 1. 检查是否在 .gitignore 中
git check-ignore -v filename

# 2. 如果被错误忽略，在 .gitignore 中添加例外
!filename

# 3. 强制添加（谨慎使用）
git add -f filename
```

### 问题：敏感文件意外被提交了

```bash
# 1. 从Git中删除但保留本地文件
git rm --cached filename

# 2. 确保文件在 .gitignore 中
echo "filename" >> .gitignore

# 3. 提交更改
git add .gitignore
git commit -m "Remove sensitive file from tracking"
```

### 问题：已经提交的敏感文件

如果敏感文件已经被提交到远程仓库：

1. **立即更改密码/密钥**
2. 使用 `git filter-branch` 或 `BFG Repo-Cleaner` 清理历史
3. 强制推送（需要团队协调）

**⚠️ 警告**：一旦推送到远程，就应该认为密码已经泄露，必须立即更改。

## 参考文档

- Git官方文档：https://git-scm.com/docs/gitignore
- GitHub .gitignore模板：https://github.com/github/gitignore

